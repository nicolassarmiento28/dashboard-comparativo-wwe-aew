# URL base del backend (Railway).
# En local no necesitas esto — Vite usa el proxy de vite.config.js.
# En Vercel, ponla como variable de entorno en el dashboard.
VITE_API_URL=https://TU-APP.railway.app/api
