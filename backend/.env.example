# Puerto del servidor (Railway lo inyecta automáticamente)
PORT=3001

# Dominio del frontend en Vercel — restringe CORS en producción.
# Acepta múltiples dominios separados por coma, o * para permitir todos.
CORS_ORIGIN=https://TU-APP.vercel.app
